<%= form_with(model: products_group) do |form| %>
  <% if products_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(products_group.errors.count, "error") %> prohibited this products_group from being saved:</h2>

      <ul>
        <% products_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :number_group %>
    <%= form.text_field :number_group %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
